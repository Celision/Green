package test

poly Expression {
    Int(int)
    DoubleInt(int, int)
    Add($, $)
}

poly (Expression) get() int {
    Add(x, y) -> {
        return x.get() + y.get()
    }
    Int(x) -> {
        return x
    }
    DoubleInt(x, y) -> {
        if x > y {
            return x
        } else {
            return y
        }
    }
}

func main()() {
    var e Expression = Expression::DoubleInt{1, 2}
    for ;; {

    }
}